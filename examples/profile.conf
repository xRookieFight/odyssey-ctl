# Example profile for odyssey-ctl(1).
#
# Copy to ~/.config/odyssey-ctl/profile.conf and adjust.  Feature and value
# names are the ones "odyssey-ctl features" prints.

brightness = 75
contrast = 75
color-preset = 6500k
volume = 40

# Uncomment to force an input on login.
# input = dp-1
